#include <stdio.h>

/**
 * add - functions that finds the sum of two numbers
 * Return - the sum
 * @a - number to be added
 * @b - number to be added
*/

int add(int a, int b)
{
    int sum;

    sum = a + b;
    return (sum);

}