#include <stdio.h>

/**
 * div - divides tow numbers
 * @a - num to be divided
 * @b - number to be divided
 * Return - the value
*/

int div (int a, int b)
{
    int div;

    div = a / b;
    return (div);
}